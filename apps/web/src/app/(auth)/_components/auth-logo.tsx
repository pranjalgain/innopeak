import { InnoPeakLogo } from "@/assets/icons/innopeak-logo";

/** Brand lockup shown above the card on every `(auth)` screen. */
export function AuthLogo() {
  return <InnoPeakLogo className="h-14 w-auto" />;
}
